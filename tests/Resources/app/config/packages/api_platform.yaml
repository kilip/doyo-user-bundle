doyo_user:
    user_class: App\Entity\User

api_platform:
    swagger:
        api_keys:
            apiKey:
                name: Authorization
                type: header
    mapping:
        paths:
            - '%kernel.project_dir%/src/Entity'
